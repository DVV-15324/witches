package routers

func initModule(modules *Modules) {
	gen.
	// Routes will be registered here
	AddTag("book", "Book endpoints")
	modules.Book.RegisterPublicRoutes(gen)
	modules.Book.RegisterProtectedRoutes(gen)

}
